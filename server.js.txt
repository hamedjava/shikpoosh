import connectDB from "./config/db.js";
const express = require("express");
const connectDB = require("./db");

const app = express();

// اتصال به دیتابیس
connectDB();

app.get("/", (req, res) => {
    res.send("سرور بالا است و به دیتابیس متصل شد!");
});

const PORT = 5000;
app.listen(PORT, () => console.log(`🚀 سرور روی پورت ${PORT} اجرا شد`));